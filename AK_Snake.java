import java.util.List;

/**
 * Write a description of class AK_Snake here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class AK_Snake 
{
    //This property is used to hold the snakeBody elements.
    List snakeBody;
    /**
     * Constructor for objects of class AK_Snake
     */
    public AK_Snake() {
        // initialise instance variables        
    }
    /**
     * This method will grow snakeBody if snake head touches the Prey.
     *
     * @param  snakeBody This is original snake body.
     * @return snakeBody This is grown snake body.
     */
    public List growSnakeBody(List snakeBody){
        //ToDo - implementation to check if snake head touches the Prey and then
        //modify the snake body.
        return snakeBody;
    }
}
